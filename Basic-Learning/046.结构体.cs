namespace DefaultNamespace;
// 结构体就是把某一类信息进行整合，组成一个新的数据类型，相当于一个新的信息
public class 046_结构体 {
    static void Main(string[] args)
    {
        struct <name>
        {
            访问权限 type typename;
            访问权限 type typename;
        }
    }
}